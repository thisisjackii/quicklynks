# File: /quicklynks/.env.example
# This is an example file. Copy it to .env and fill in your values.
# DO NOT COMMIT .env TO VERSION CONTROL.

# Backend Configuration
GIN_MODE=debug # debug, release, test
SERVER_PORT=8080
SECRET_KEY=a_very_secret_key_that_should_be_long_and_random

# Database Configuration
DB_HOST=db
DB_PORT=5432
DB_USER=quicklynks_user
DB_PASSWORD=quicklynks_password
DB_NAME=quicklynks_db
DB_SSL_MODE=disable

# Frontend Configuration (exposed to the browser)
# Note: In SvelteKit, prefix with PUBLIC_
PUBLIC_API_BASE_URL=http://localhost:8080